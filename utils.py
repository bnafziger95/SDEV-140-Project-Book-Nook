# utility functions for project NO WINDOW

# import json
# define functions
# load_library() --> loads library.json and returns data
# save_library(data) --> saves data to library.json
# validate_input(title, author, genre, rating, status) --> validates input fields
# show_message(message) --> displays message to user
# format_book_entry(book) --> formats book entry for display
# get_genre_options() --> returns list of genre options for dropdown
# get_status_options() --> returns list of status options for dropdown
# get_rating_options() --> returns list of rating options for dropdown